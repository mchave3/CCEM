<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="CCEM.Views.Modules.SccmToolsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:CCEM.ViewModels.Modules"
      mc:Ignorable="d"
      x:DataType="vm:SccmToolsViewModel">
    <Grid Padding="{ThemeResource ContentPagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    Spacing="8">
            <Button Command="{x:Bind ViewModel.RunSelectedToolCommand}"
                    Content="Run selected tool" />
            <ProgressRing Width="20"
                          Height="20"
                          IsActive="{x:Bind ViewModel.IsLoading}" />
        </StackPanel>

        <TextBlock Grid.Row="1"
                   Opacity="0.7"
                   Text="{x:Bind ViewModel.StatusMessage}" />

        <Pivot Grid.Row="2">
            <PivotItem Header="Tools">
                <ListView ItemsSource="{x:Bind ViewModel.Tools}"
                          SelectedItem="{x:Bind ViewModel.SelectedTool, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:SccmToolRow">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold"
                                               Text="{x:Bind Title}" />
                                    <TextBlock Opacity="0.7"
                                               Text="{x:Bind Description}" />
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Tag="{x:Bind}"
                                        Click="RunTool_Click"
                                        Content="Run" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>

            <PivotItem Header="Scripts">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal"
                                Spacing="8">
                        <Button Command="{x:Bind ViewModel.RefreshScriptsCommand}"
                                Content="Refresh scripts" />
                        <Button Command="{x:Bind ViewModel.OpenScriptsFolderCommand}"
                                Content="Open scripts folder" />
                        <Button Command="{x:Bind ViewModel.RunSelectedScriptCommand}"
                                Content="Run selected script" />
                    </StackPanel>

                    <TextBlock Grid.Row="1"
                               Opacity="0.7"
                               Text="{x:Bind ViewModel.ScriptRoot}" />

                    <Grid Grid.Row="2"
                          ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="3*" />
                        </Grid.ColumnDefinitions>

                        <ListView ItemsSource="{x:Bind ViewModel.Scripts}"
                                  SelectedItem="{x:Bind ViewModel.SelectedScript, Mode=TwoWay}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="vm:SccmScriptRow">
                                    <StackPanel Spacing="2">
                                        <TextBlock FontWeight="SemiBold"
                                                   Text="{x:Bind Name}" />
                                        <TextBlock Opacity="0.7"
                                                   Text="{x:Bind RelativePath}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <ScrollViewer Grid.Column="1"
                                      HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Auto">
                            <TextBlock Text="{x:Bind ViewModel.ScriptOutput}"
                                       TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Grid>

                    <TextBlock Grid.Row="3"
                               Opacity="0.7"
                               Text="Scripts execute on the target via the active SCCM connection (WSMan)." />
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
