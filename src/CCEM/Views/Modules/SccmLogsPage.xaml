<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="CCEM.Views.Modules.SccmLogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:CCEM.ViewModels.Modules"
      mc:Ignorable="d"
      x:DataType="vm:SccmLogsViewModel">

    <Grid Padding="{StaticResource ContentPagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Margin="{StaticResource PageHeaderMargin}">
            <TextBlock Style="{StaticResource PageTitleStyle}"
                       Text="Log Viewer" />
            <TextBlock Style="{StaticResource PageSubtitleStyle}"
                       Text="View SCCM client log files from the remote computer." />
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource InfoBarAreaStyle}">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Text="{x:Bind ViewModel.FilePath, Mode=TwoWay}"
                             PlaceholderText="Enter log file path (e.g., C:\Windows\CCM\Logs\PolicyAgent.log)"
                             Header="Log File Path" />

                    <NumberBox Grid.Column="1"
                               Width="100"
                               Value="{x:Bind ViewModel.TailLines, Mode=TwoWay}"
                               SpinButtonPlacementMode="Compact"
                               Minimum="1"
                               Maximum="5000"
                               Header="Lines" />

                    <Button Grid.Column="2"
                            Style="{StaticResource PrimaryCommandButtonStyle}"
                            Command="{x:Bind ViewModel.LoadCommand}"
                            VerticalAlignment="Bottom">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE7C3;" FontSize="14" />
                            <TextBlock Text="Load" />
                        </StackPanel>
                    </Button>

                    <ProgressRing Grid.Column="3"
                                  Width="20" Height="20"
                                  IsActive="{x:Bind ViewModel.IsLoading}"
                                  VerticalAlignment="Bottom"
                                  Margin="0,0,0,8" />
                </Grid>

                <TextBlock Grid.Row="1"
                           Style="{StaticResource StatusTextStyle}"
                           Text="{x:Bind ViewModel.StatusMessage}" />
            </Grid>
        </Border>

        <!-- Log Content -->
        <Border Grid.Row="2"
                Style="{StaticResource CardStyle}"
                Margin="0,8,0,0"
                Padding="0">
            <ListView ItemsSource="{x:Bind ViewModel.Lines}"
                      SelectionMode="None"
                      Padding="12">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock FontFamily="Cascadia Code, Consolas, Courier New"
                                   FontSize="12"
                                   Text="{Binding}"
                                   TextWrapping="WrapWholeWords"
                                   IsTextSelectionEnabled="True" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>

