name: 🌙 Build Nightly

on:
  schedule:
    - cron: '0 2 * * *' # 02:00 UTC daily
  workflow_dispatch:

permissions:
  contents: write

jobs:
  nightly:
    name: Nightly
    uses: ./.github/workflows/build-and-release.yml
    with:
      source-branch: develop
      release-channel: nightly
      release-type: nightly
      release-title-prefix: 'CCEM Nightly'
    secrets: inherit
